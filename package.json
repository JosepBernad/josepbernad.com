{
  "name": "josepbernad.com",
  "version": "1.13.0",
  "description": "Josep Bernad - Live & DJ Set",
  "private": true,
  "scripts": {
    "dev": "npm run build:presskit && eleventy --serve",
    "build": "npm run build:presskit && eleventy",
    "build:favicons": "node scripts/generate-favicons.js",
    "build:rider": "node scripts/build-rider.js",
    "build:presskit": "node scripts/build-presskit.js",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "vitest run && playwright test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@playwright/test": "^1.59.1",
    "archiver": "^7.0.1",
    "opentype.js": "^1.3.4",
    "pdfkit": "^0.18.0",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5",
    "vitest": "^4.1.4",
    "wawoff2": "^2.0.1"
  }
}
