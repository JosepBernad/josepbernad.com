{
  "name": "josepbernad.com",
  "version": "1.4.2",
  "description": "Josep Bernad - Live & DJ Set",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "vitest run && playwright test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@playwright/test": "^1.59.1",
    "vitest": "^4.1.4"
  }
}
